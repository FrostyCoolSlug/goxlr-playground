mod buttons;
pub mod channels;
pub mod colours;
pub mod encoders;
pub mod faders;
pub mod interaction;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
